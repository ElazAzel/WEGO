import { WButton, WCard, WChip } from "@wego/ui";
import { PageHeader } from "../../components/layout/PageHeader";
import { useAppStore } from "../../store/use-app-store";

export function SettingsPage() { const space = useAppStore((state) => state.space); const homeVariant = useAppStore((state) => state.homeVariant); const revealVariant = useAppStore((state) => state.revealVariant); const setStyle = useAppStore((state) => state.setStyle); const setHomeVariant = useAppStore((state) => state.setHomeVariant); const setRevealVariant = useAppStore((state) => state.setRevealVariant); if (!space) return null; return <div className="app-page"><PageHeader eyebrow="Настройки" title="Ваш Wego" description="Настройте настроение маленького мира." /><div className="settings-list"><WCard tone="paper"><div className="w-mono-caps">Визуальный стиль</div><div className="chip-wrap settings-options"><WChip active={space.style === "a"} onClick={() => setStyle("a")}>Style A · Premium</WChip><WChip active={space.style === "b"} onClick={() => setStyle("b")}>Style B · Cozy</WChip></div></WCard><WCard tone="paper"><div className="w-mono-caps">Главный экран</div><div className="chip-wrap settings-options"><WChip active={homeVariant === "v1"} onClick={() => setHomeVariant("v1")}>Room-first</WChip><WChip active={homeVariant === "v2"} onClick={() => setHomeVariant("v2")}>Character-first</WChip></div></WCard><WCard tone="paper"><div className="w-mono-caps">Reveal</div><div className="chip-wrap settings-options"><WChip active={revealVariant === "v1"} onClick={() => setRevealVariant("v1")}>Parallel cards</WChip><WChip active={revealVariant === "v2"} onClick={() => setRevealVariant("v2")}>Envelope</WChip></div></WCard></div></div>; }
