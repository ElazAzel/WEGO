import { WCard } from "@wego/ui";

export function HomeSummary({ daysTogether, nextEvent, myAnswered, partnerAnswered, partnerName }: { daysTogether: number; nextEvent: { title: string; dateLabel: string }; myAnswered: boolean; partnerAnswered: boolean; partnerName?: string }) {
  return <section className="home-summary screen-padding" aria-label="Сводка нашей жизни"><WCard tone="lilac" className="home-summary__hero"><div className="w-mono-caps">Мы · общий экран</div><div className="home-summary__counter"><strong>{daysTogether.toLocaleString("ru-RU")}</strong><span>дней вместе</span></div><div className="home-summary__stats"><div><strong>{myAnswered ? "Готово" : "Открыто"}</strong><span>твой<br />чек-ин</span></div><div><strong>{partnerAnswered ? "Здесь" : "Ждём"}</strong><span>{partnerName ?? "партнёра"}</span></div><div><strong>{myAnswered && partnerAnswered ? "Открыт" : "—"}</strong><span>сегодняшний<br />Reveal</span></div></div></WCard><WCard tone="paper" className="home-summary__next"><div><div className="w-mono-caps">Следующая дата</div><div className="life-title">{nextEvent.title}</div><small>{nextEvent.dateLabel}</small></div><span className="record-dot" /></WCard></section>;
}
