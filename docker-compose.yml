services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: wego
      POSTGRES_USER: wego
      POSTGRES_PASSWORD: wego
    ports:
      - "5432:5432"
    volumes:
      - wego-postgres:/var/lib/postgresql/data

volumes:
  wego-postgres:
